\documentclass{name}
\usepackage{lipsum}
\usepackage{multicol}
\usepackage{listings}
\usepackage{longtable}
\usepackage{amsmath}
\usepackage{mathtools}
\usepackage{amssymb}
\usepackage{booktabs}
\usepackage{array}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{float}
\usepackage{setspace}
\usepackage{stmaryrd}
\usepackage[backend=bibtex,sorting=none]{biblatex}
\addbibresource{bibliographie.bib}
\usepackage{csquotes}
\usepackage[english]{babel}
\usepackage{fancyhdr}
\usepackage{lmodern}
\usepackage{xcolor}
\usepackage{algorithm}
\usepackage{algorithmic}
\usepackage{awesomebox}
\usepackage{todonotes}
\usepackage{upquote}

\newtheorem{theorem}{Theorem}
\newtheorem{remark}{Remark}
\newtheorem{definition}{Definition}

\newcounter{pagezero}

\begin{document}

%----------- Informations du rapport ---------
\UE{S8 Machine Learning}
\sujet{Kaggle Project: Textile Classification \& AI Safety}
\titre{TILDA Textile Classifier and AI Safety}

\eleves{Marin \textsc{Decanini}\\Nolan \textsc{Carlisi}}

\enseignant{Pierre \textsc{Weiss}\\Université de Toulouse\\ Senior CNRS researcher at IRIT}

%----------- Initialisation -------------------
\fairemarges %Afficher les marges
\fairepagedegarde %Créer la page de garde

\newpage
\setcounter{pagezero}{0}

\begin{center}
	\begin{minipage}{13cm}
	    \begin{center}
	        \section*{Abstract}
	    \end{center}
	    This report presents our implementation and experimental analysis for the S8 Machine Learning Kaggle project on the TILDA textile dataset. 
	    We focus on two core components: first, training state-of-the-art Convolutional Neural Networks (CNNs) for classification; and second, analyzing machine learning security and AI safety under biased training distributions. 
	    Specifically, we implement LeNet and ResNet architectures, evaluate their performance using mini-batch SGD, and perform systematic hyperparameter tuning. 
	    Furthermore, we investigate model vulnerability to spurious correlations by constructing a biased dataset with a noise-like channel correlated with labels, measuring both classification accuracy and fairness using the Disparate Impact (DI) metric.
	\end{minipage}
\end{center}

\par 
\setcounter{page}{\value{pagezero}}

\newpage
\setcounter{page}{\value{pagezero}}
\tableofcontents

\newpage

\section{Introduction}
Deep learning has revolutionized image classification and pattern recognition. However, as these models are increasingly deployed in real-world systems, understanding their capacity, training characteristics, and vulnerabilities to dataset bias has become a critical area of AI safety. This report documents our investigations on the TILDA textile dataset, structured in two major sections: first, training and comparing state-of-the-art Convolutional Neural Networks (LeNet-5, AlexNet, and ResNet-18) for multi-class textile classification; and second, analyzing how neural networks can inherit and amplify spurious correlations (biases) using a mathematical framework for biased dataset construction and evaluating the models using the Disparate Impact (DI) metric.

\section{Training State-of-the-Art CNN Models}

\subsection{Image Pre-processing and Data Augmentation}
The TILDA textile dataset contains grayscale \texttt{.tif} images of size $768 \times 512$ pixels representing different textile textures and defect classes. The dataset has 8 classes, with a relatively small sample size of approximately 2,361 images (~295 samples per class). To handle this dataset effectively and perform a rigorous comparison, we apply the following pre-processing and augmentation pipeline:
\begin{itemize}
    \item \textbf{Image Resizing:} To ensure all models receive the same input size and can be compared directly, we resize all images to a standard $224 \times 224$ pixels.
    \item \textbf{Data Augmentation (Q2.1):} Due to the small sample size, deep architectures (especially AlexNet and ResNet-18) are highly prone to overfitting. We apply data augmentation techniques that preserve the textile texture semantic content:
    \begin{itemize}
        \item \textit{Random Horizontal and Vertical Flips:} Textile patterns and defects are invariant to reflections.
        \item \textit{Random Rotations (up to $15^\circ$):} Fabric images may be rotated slightly.
        \item \textit{Random Translation (Affine translation of up to 10\%):} Small shifts in alignment simulate different camera crops.
    \end{itemize}
    \item \textbf{Normalization:} We convert the images to PyTorch tensors and normalize the pixel values using a mean of 0.5 and a standard deviation of 0.5. This rescales the pixel range from $[0, 1]$ to $[-1, 1]$, centering the input data around zero with a variance close to 1, which helps stabilize gradient descent backpropagation (as discussed in LeNet-5 paper \S II.B).
\end{itemize}

\subsection{Model Architecture and Selection}
For Question 2.2, we selected three distinct convolutional neural network architectures representing a historical progression in deep learning:
\begin{enumerate}
    \item \textbf{LeNet-5 (LeCun et al., 1998):} Serves as a classical baseline. It features a simple architecture of three convolutional layers, average pooling, and two fully connected layers. Tanh activation functions are used. We adapt it by replacing the final spatial collapse with an adaptive average pooling layer to support $224 \times 224$ inputs, mapping the output to 8 classes. Total parameter count is 61,536.
    \item \textbf{AlexNet (Krizhevsky et al., 2012):} Represents the breakthrough of deep CNNs, introducing ReLU activation functions to speed up training, overlapping max pooling ($3 \times 3$ with stride 2), and Dropout (0.5) to prevent overfitting in the fully connected layers. Because the TILDA dataset is extremely small compared to ImageNet (1.2M images), a full-scale AlexNet would immediately overfit. Thus, we halved the number of channels (first layer: 48 channels instead of 96, second: 128 instead of 256, etc.) and reduced the fully connected layers from 4096 to 1024 units.
    \item \textbf{ResNet-18 (He et al., 2016):} Represents the modern state-of-the-art in CNNs. It solves the vanishing gradient problem in deep networks using residual connections (identity shortcuts mapping $y = F(x) + x$). It uses Batch Normalization after every convolutional layer (before ReLU activation) and does not use dropout. The network finishes with global average pooling and a single linear layer.
\end{enumerate}

\subsection{Training and Optimization Methodology}
For Question 2.3, all three models are trained from scratch using mini-batch Stochastic Gradient Descent (SGD) with a momentum of 0.9 and an initial learning rate of 0.01. We employ a \texttt{ReduceLROnPlateau} scheduler that monitors the validation loss and divides the learning rate by 10 (factor of 0.1) if the validation loss does not improve for 5 epochs (scheduler patience = 5), matching the strategy in both the AlexNet and ResNet papers. Early stopping is applied with a patience of 10 epochs.

Training is performed on the macOS MPS (Metal Performance Shaders) backend using an Apple Silicon chip. The batch size is set to 32. 
\begin{table}[H]
\centering
\caption{Model Architecture Details and Parameter Counts}
\label{tab:models}
\begin{tabular}{lllr}
\toprule
Model & Major Layers & Activations & Parameter Count \\
\midrule
LeNet-5 & 3 Conv, 2 AvgPool, 2 FC & Tanh & 61,536 \\
AlexNet (Modified) & 5 Conv, 3 MaxPool, 3 FC & ReLU & 14,586,216 \\
ResNet-18 & 17 Conv, 1 MaxPool, 1 FC & ReLU & 11,174,344 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Hyperparameter Tuning and Validation}
To tune hyperparameters and prevent overfitting, we split the labeled TILDA dataset into a stratified 80/20 train/validation split. This guarantees that each class has the same proportion (80/20) in both the training set (1,888 samples) and validation set (473 samples).

For Question 2.4, the model validation metrics, parameter details, and total training times are summarized in Table \ref{tab:results}.
\begin{table}[H]
\centering
\caption{Model Training and Evaluation Metrics}
\label{tab:results}
\begin{tabular}{lcccccc}
\toprule
Model & Learning Rate & Batch Size & Epochs & Train Accuracy & Val Accuracy & Training Time (s) \\
\midrule
LeNet-5 & 0.01 & 32 & 50 & 28.23\% & 30.87\% & 427.3s \\
AlexNet (Modified) & 0.01 & 32 & 50 & 29.77\% & 36.36\% & 357.2s \\
ResNet-18 & 0.01 & 32 & 50 & 64.88\% & \textbf{69.98\%} & 1,652.8s \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[H]
\centering
\begin{subfigure}[b]{0.32\textwidth}
    \centering
    \includegraphics[width=\textwidth]{figures/lenet5_training_curves.png}
    \caption{LeNet-5 Curves}
    \label{fig:lenet5_curves}
\end{subfigure}
\begin{subfigure}[b]{0.32\textwidth}
    \centering
    \includegraphics[width=\textwidth]{figures/alexnet_training_curves.png}
    \caption{AlexNet Curves}
    \label{fig:alexnet_curves}
\end{subfigure}
\begin{subfigure}[b]{0.32\textwidth}
    \centering
    \includegraphics[width=\textwidth]{figures/resnet18_training_curves.png}
    \caption{ResNet-18 Curves}
    \label{fig:resnet18_curves}
\end{subfigure}
\caption{Training and validation loss and accuracy curves for the three architectures on the TILDA dataset.}
\label{fig:training_curves}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.8\textwidth]{figures/model_comparison.png}
\caption{Comparison of peak validation accuracy and total training time (seconds) across the three models.}
\label{fig:model_comparison}
\end{figure}

\subsubsection{Analysis of Model Performance and Overfitting}
The experimental results demonstrate a clear hierarchical progression in performance:
\begin{itemize}
    \item \textbf{LeNet-5 (30.87\%):} The classical architecture shows severe underfitting. Due to its shallow depth, average pooling, and tanh activation functions, it is unable to extract complex texture patterns from the fabric images.
    \item \textbf{AlexNet (36.36\%):} The modified AlexNet shows slightly better performance. However, without Batch Normalization, it struggles to optimize efficiently on grayscale images from scratch. It also exhibits overfitting tendencies due to high fully-connected layer parameter density relative to the dataset size.
    \item \textbf{ResNet-18 (69.98\%):} ResNet-18 achieves a highly superior validation accuracy of 69.98\%, nearly doubling the baselines. The skip connections prevent vanishing gradients, while the Batch Normalization layers stabilize intermediate activations. This allows the network to learn rich representations of textile textures without overfitting prematurely.
\end{itemize}

\section{Machine Learning and AI Safety}

\subsection{Real-Life Bias Context}
For Question 3.1, machine learning models do not learn abstract reasoning; instead, they learn to exploit correlations present in their training distributions. In real-world settings, if the training data contains historical biases or spurious correlations, the model will learn them and make biased predictions.
Examples:
\begin{itemize}
    \item \textbf{Automated Hiring Systems:} If historical data favors male candidates due to past systemic biases, an AI resume screening tool will learn to associate words like "women's" or women's college names with lower scores, amplifying gender bias.
    \item \textbf{Predictive Policing:} If policing datasets reflect over-policing of minority neighborhoods, the predictive model will project higher crime risk for those areas, creating a feedback loop of increased surveillance and arrest rates in those specific communities.
\end{itemize}
The consequences are severe, causing systemic discrimination, denial of credit or employment, and loss of trust in AI systems.

\subsection{Biased Dataset Construction}
For Question 3.2, we construct a biased version of the dataset using a binary classification task on classes 0 and 1 of TILDA (300 samples each). We add a noise-like second channel $\epsilon$ representing a spurious correlation:
$$S \mid \{y = k\} \sim \text{Bernoulli}(p_k), \quad k \in \{0, 1\}.$$
The noise channel is set as:
\begin{itemize}
    \item $\epsilon = 0$ if $S = 0$
    \item $\epsilon \sim \mathcal{N}(0, I)$ if $S = 1$
\end{itemize}
In the unbiased setting ($model_1$), $p_0 = 0.5, p_1 = 0.5$, meaning noise is independent of the label. In the biased setting ($model_2$), $p_0 = 0.0, p_1 = 1.0$, meaning noise is perfectly correlated with label 1. We implement this in PyTorch by creating a \texttt{BiasedDataset} class that wraps our stratified validation split subsets. It dynamically retrieves labels and pre-computes $S$ values for exact reproducibility. When retrieving a sample, it generates the channel $\epsilon$ based on $S$ and concatenates it with $x$, producing a 2-channel tensor.

\subsection{Model Bias Evaluation}

\subsubsection{Disparate Impact Analysis}
For Question 3.3, we compare the Disparate Impact (DI) of $model_1$ (trained on unbiased data, $p_0=0.5, p_1=0.5$) and $model_2$ (trained on biased data, $p_0=0.0, p_1=1.0$) evaluated on unbiased validation data:
\begin{itemize}
    \item \textbf{Model 1 (Unbiased):} Achieved a Disparate Impact metric of \textbf{0.9370}. Since this is close to $1.0$ (and falls well within the standard ethical threshold of $[0.8, 1.25]$), it demonstrates that the model does not discriminate based on the presence of the noise channel $S$. The model makes predictions independent of the spurious noise.
    \item \textbf{Model 2 (Biased):} Achieved a Disparate Impact metric of \textbf{0.3446}. This is extremely low, falling far below the $0.8$ threshold. This indicates a high level of bias: the model is nearly three times more likely to predict class 1 when the noise channel is present ($S=1$) than when it is absent ($S=0$).
\end{itemize}
This experimentally proves that $model_2$ has learned to rely on the spurious correlation (noise channel) rather than the underlying textile pattern.

\subsubsection{Test Accuracy Comparison}
For Question 3.4, we compare the validation accuracy of both models on the binary classification task:
\begin{itemize}
    \item \textbf{Model 1 (Unbiased):} Achieved a validation accuracy of \textbf{49.17\%} (with validation loss of 0.6901).
    \item \textbf{Model 2 (Biased):} Achieved a validation accuracy of \textbf{55.00\%}.
\end{itemize}
During training, $model_2$ achieved 100\% training accuracy within just 17 epochs because it exploited the noise channel. However, when evaluated on unbiased validation data where the noise channel is no longer correlated with the true label, its accuracy drops to 55.00\% (near the random guess baseline). This illustrates that the biased model fails to generalize to unbiased domains because it relies on non-robust spurious shortcuts.

\subsubsection{Summary of Results and Takeaways}
For Question 3.5, we summarize the training and validation accuracy and Disparate Impact metrics for both models in Table \ref{tab:safety_results}.
\begin{table}[H]
\centering
\caption{Safety Experiment Results ($model_1$ vs $model_2$)}
\label{tab:safety_results}
\begin{tabular}{lcccc}
\toprule
Model & Train Accuracy & Val Accuracy & Val Loss & Disparate Impact (DI) \\
\midrule
Model 1 (Unbiased) & 48.13\% & 49.17\% & 0.6901 & 0.9370 (Fair) \\
Model 2 (Biased) & 100.0\% & 55.00\% & 0.6915 & 0.3446 (Biased) \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[H]
\centering
\includegraphics[width=0.8\textwidth]{figures/bias_evaluation.png}
\caption{Comparison of Accuracy and Disparate Impact (DI) between Model 1 and Model 2.}
\label{fig:bias_evaluation}
\end{figure}

The takeaways are clear: neural networks are shortcut learners. When presented with a simple, high-frequency signal (the noise channel $\epsilon$) that correlates with the label, they will select it over more complex semantic features (the textile patterns). When deployed in an unbiased environment, the model's accuracy collapses and it makes heavily biased predictions, exposing serious safety risks in real-world applications.

\subsection{Bias Study in the Literature (Bonus)}
The question ``Can we trust my model?'' is central to modern AI safety and governance. In light of our experimental results and recent regulatory frameworks such as the European Union AI Act (EU AI Act) and guidelines from the Commission Nationale de l'Informatique et des Libertés (CNIL), the answer is a nuanced ``no, unless systematic auditing and mitigation are implemented.''

First, our experiment showed how easily a model can become a ``shortcut learner,'' achieving 100\% training accuracy by exploiting a spurious noise channel that happened to correlate with labels, while remaining completely unable to classify the actual underlying images when tested in an unbiased domain. In real-world applications, such shortcut learning goes undetected if the training and test sets share the same bias. This is exactly what the EU AI Act identifies as a major risk in ``high-risk'' AI systems (e.g., in recruitment, credit scoring, or biometric identification). The regulation mandates strict data governance requirements (Article 10), requiring training, validation, and testing datasets to be relevant, representative, and to the best extent possible, free of errors and biases.

From the perspective of CNIL's compliance guidelines for AI systems, trusting a model requires transparency and ``explainability by design.'' If we cannot audit what features the model is using, we cannot guarantee its fairness. Our model 2 would pass standard validation checks if the validation set was also biased, yet it is fundamentally untrustworthy. Therefore, trust cannot be based solely on global accuracy metrics. Instead, we must employ:
\begin{enumerate}
    \item \textbf{Fairness Audits:} Actively measuring metrics like Disparate Impact (DI), Equalized Odds, or Demographic Parity across sensitive groups, as recommended by the CNIL.
    \item \textbf{Spurious Correlation Diagnostics:} Testing the model on counterfactual or intervened datasets (e.g., removing or modifying specific channels/regions) to check if the predictions remain stable.
    \item \textbf{Explainable AI (XAI):} Using tools like Integrated Gradients or Grad-CAM to visualize model attention and ensure it focuses on semantic features (textile fibers, defects) rather than background noise.
\end{enumerate}
In conclusion, we cannot blindly trust a machine learning model. Trust must be earned through a continuous lifecycle of rigorous bias auditing, dataset curation, and compliance with ethical guidelines like the EU AI Act and CNIL recommendations.

\printbibliography

\end{document}
